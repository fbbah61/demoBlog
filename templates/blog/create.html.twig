{% extends "base.html.twig" %}

{% form_theme formArticle  'bootstrap_4_layout.html.twig' %}


{% block body %}

<h1 class="display-4 text-center">Creation d' un article </h1>
<div class="container">
    {# <form method="post" class="col-md-8 mx-auto">
        <div class="form-group">
          <label for="title">Titre</label>
          <input type="text" class="form-control" id="title" name="title" >
        </div>
        <div class="form-group">
          <label for="content">Content</label>
          <textarea type="text" class="form-control" id="content" name="content" ></textarea>
        </div>
        <div class="form-group">
          <label for="image">Image</label>
          <input type="text" class="form-control" id="image" name="image" >
        </div>
      <button class="btn btn-primary btn-lg btn-block" type="submit">Enregistrer</button>
    </form>
  </div> #}
  {# {{ form_(formArticle)}} #}

  {# {{ form_start(formArticle)}}

      {{ form_label(formArticle.title, 'Titre')}}
      {{ form_widget(formArticle.title) }}

      {{ form_label(formArticle.content, "Contenu") }}
      {{ form_widget(formArticle.content) }}

      {{ form_label(formArticle.image, "URL de l'image") }}
      {{ form_widget(formArticle.image) }}

  {{ form_end(formArticle)}} #}

  {{ form_start(formArticle,{ 'attr' : {'class': "col-md-7 mx-auto"}}) }}

      {{ form_row(formArticle.title, { 'attr' : {'placeholder': "Titre de l'article",}}) }}

      {{ form_row(formArticle.category) }}

      {{ form_row(formArticle.content, { 'attr' : {'placeholder': "Contenu de l'article", "rows": 5}}) }}

      {{ form_row(formArticle.image, { 'attr' : {'placeholder': "url de l'article"}}) }}

    <button type="submit" class="btn btn-primary">
     {% if editMode %}
      Enregistrer les  modifications
     {% else %}
        ajouter l' article
     {% endif %}
    </button>
  {{ form_end(formArticle)}}



</div>

{% endblock %}